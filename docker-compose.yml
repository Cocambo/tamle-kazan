services:
  db-user:
    image: postgres:15
    container_name: db-user
    restart: always
    env_file:
      - ./backend/user-service/.env
    ports:
      - "5432:5432"
    volumes:
      - db-user-data:/var/lib/postgresql/data
    networks:
      - tamle-net
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres || exit 1" ]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s

  user-service:
    build:
      context: ./backend/user-service
      dockerfile: Dockerfile
    container_name: user-service
    depends_on:
      db-user:
        condition: service_healthy
    env_file:
      - ./backend/user-service/.env
    ports:
      - "8081:8081"
    networks:
      - tamle-net

  db-restaurant:
    image: postgres:15
    container_name: db-restaurant
    restart: always
    env_file:
      - ./backend/restaurant-service/.env
    ports:
      - "5433:5432"
    volumes:
      - db-restaurant-data:/var/lib/postgresql/data
    networks:
      - tamle-net
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres || exit 1" ]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s

  restaurant-service:
    build:
      context: ./backend/restaurant-service
      dockerfile: Dockerfile
    container_name: restaurant-service
    depends_on:
      db-restaurant:
        condition: service_healthy
    env_file:
      - ./backend/restaurant-service/.env
    ports:
      - "8082:8082"
    volumes:
      - ./backend/restaurant-service/photos:/app/photos
    networks:
      - tamle-net

  api-gateway:
    build:
      context: ./backend/api-gateway-service
      dockerfile: Dockerfile
    container_name: api-gateway-service
    depends_on:
      - user-service
      - restaurant-service
    env_file:
      - ./backend/api-gateway-service/.env
    ports:
      - "8080:8080"
    networks:
      - tamle-net

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: frontend
    depends_on:
      - api-gateway
    ports:
      - "5173:80"
    networks:
      - tamle-net
    # volumes:
    #   - ./frontend:/app
    #   - /app/node_modules


networks:
  tamle-net:
    driver: bridge

volumes:
  db-user-data:
  db-restaurant-data:
